What the project is
“REST API for cafe table booking with roles: customer, owner, admin.”

Tech stack
Example: “Built with Node.js, Express.js, MySQL, JWT authentication.”

How to run it:
Commands to run locally:
git clone <repo-url>
cd cafe-booking-api
npm install
# create .env using .env.example
npm start
